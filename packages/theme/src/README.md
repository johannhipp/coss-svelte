# packages/theme/src

Token and Tailwind CSS v4 source for the shared coss-svelte theme package.

`style-coss.css` defines the consumer-safe shared theme variables, light and dark surfaces, component class mappings, and motion tokens. Docs-only styles live in the docs app stylesheet.
