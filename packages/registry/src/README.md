# packages/registry/src

This package contains the registry metadata and schema consumed by the
repository registry generator. Generated installable item JSON belongs under
`apps/registry/static`; keep source closure and schema logic here.
