# packages/cli

Future install/update CLI for copy-and-own workflows.

This should study shadcn-svelte's CLI before implementation. The first target is registry install compatibility before adding project-specific ergonomics.
