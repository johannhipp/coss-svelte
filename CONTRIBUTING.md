# Contributing

Implementation has not started. The first accepted contributions should be foundation work:

- token contract design
- registry metadata schema
- first component batch plan
- test strategy
- documentation architecture

When component implementation begins, use `docs/implementation/phases.md` as the ordering guide and keep each pull request scoped to a small component family.
